@model IEnumerable<SIGEM_BIDSS.Models.tbTipoSolicitud>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tipsol_Descripcion)
            </th>
   
   
            <th>ACCIONES</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.tipsol_Descripcion)
                </td>
              
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.tipsol_Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.tipsol_Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.tipsol_Id })
                </td>
            </tr>
        }
        </tbody>
</table>
